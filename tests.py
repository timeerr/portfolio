#!/usr/bin/python3

from dbhandler.dbwrapper import Results
from dbhandler.db_initialize import create_tables
from gui import app

from datetime import datetime

# path_to_db = 'portfolio.db'
#
# create_tables(path_to_db)
# r = Results(path_to_db)
#
#
# # r.addResult(datetime.now(), "DUKASFOREX", 101)
# # r.addResult(datetime.now(), "DUKASBIN", 131)
#
# # r.updateResult(2, newdate=datetime.now(), newaccount="LeLe", newamount=9999)
#
# r.deleteResult(2)
